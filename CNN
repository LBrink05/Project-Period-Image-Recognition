import numpy as np

class CNN: 
    def __init__(self, input_shape, kernel_size, depth) : 
        self.input_depth, self.input_height, self.input_width = input_shape  #the image 
        self.kernel_size = kernel_size
        self.depth = depth

        self.kernels_shape = (depth, self.input_depth, kernel_size, kernel_size) #(filters, colours , size)

        self.kernels = np.random.randn(*self.kernels_shape) * 0.1

        print(f"layer with filter of: {kernel_size}x{kernel_size}")


    
    
    



    
    
test = CNN(input_shape=(3, 384, 384), kernel_size=5, depth=16)
